[tool.poetry]
name = "esa-python"
version = "0.1.0"
description = ""
authors = ["Kosuke Mizuno (WSL, Lab PC) <mizuno.k.ae@m.titech.ac.jp>"]

[tool.poetry.dependencies]
python = "^3"
pyyaml = "*"
requests = "*"

[tool.poetry.dev-dependencies]


[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
